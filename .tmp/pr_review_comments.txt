{
  "path": "public/package.json",
  "line": null,
  "body": "The PR description states the upgrade is to Angular 21.2.10, but this package.json sets Angular deps to `^21.0.0`. That range can drift across minor releases over time and no longer reflect the stated target version. Consider pinning the Angular packages to the intended minor (e.g., `^21.2.10` or `~21.2.10`) or updating the PR description if the broader range is intentional."
}
{
  "path": "admin/package.json",
  "line": null,
  "body": "The PR description states the upgrade is to Angular 21.2.10, but this package.json sets Angular deps to `^21.0.0`. That range can drift across minor releases over time and no longer reflect the stated target version. Consider pinning the Angular packages to the intended minor (e.g., `^21.2.10` or `~21.2.10`) or updating the PR description if the broader range is intentional."
}
{
  "path": "libs/package.json",
  "line": null,
  "body": "The PR description indicates Angular 21.2.10 across shared libs as well, but this package.json uses `^21.0.0` for `@angular/common`/`@angular/core`. Consider aligning these versions to the intended Angular minor to avoid unintended minor-version drift between installs and packages."
}
{
  "path": "libs/client/typescript-ng/README.md",
  "line": null,
  "body": "This generated client README now instructs running `npm install` without `--ignore-scripts`. Elsewhere in this repo (e.g., `libs/README.md`, `api/README.md`) install steps explicitly use `--ignore-scripts`, presumably to avoid executing lifecycle scripts during local setup. Consider keeping `--ignore-scripts` here (or clearly documenting why it’s safe to remove); if this change comes from openapi-generator output, prefer adjusting generator templates/post-processing so the guidance stays consistent.\n```suggestion\nnpm install --ignore-scripts\n```"
}
{
  "path": "admin/src/core/services/cognito.service.ts",
  "line": 59,
  "body": "`init()` calls `loadRemoteConfig()` twice (once via `await` at the top, then again inside the returned `Promise`). This duplicates the network call and can cause inconsistent behavior if the second call fails after the first succeeded (e.g., leaving the service uninitialized). Reuse the already-loaded `awsCognitoConfig`/Amplify config and remove the second `loadRemoteConfig()` call."
}
{
  "path": "admin/src/app/search/search.component.ts",
  "line": 111,
  "body": "`doSearch()` triggers `cd.detectChanges()` in subscription callbacks, but the HTTP request subscription isn’t tied to `ngUnsubscribe`. If the component is destroyed during an in-flight search, callbacks can still fire and attempt change detection on a destroyed view. Pipe the search request through `takeUntil(this.ngUnsubscribe)` (and consider `finalize()` for the `searching`/`searched` flags)."
}
{
  "path": "public/src/app/applications/projects.component.ts",
  "line": 107,
  "body": "New fragment-handling logic (`handleFragment`, initial fragment check, and splash modal open-guard) isn’t covered by the existing `ProjectsComponent` unit tests. Adding tests for `splash`/`find`/`details` fragments (including initial URL fragment) would help prevent regressions in the bootstrap/fragment behavior."
}
{
  "path": "public/src/app/header/header.component.ts",
  "line": 39,
  "body": "`router.events` subscription never unsubscribes, and `distinctUntilChanged()` on `NavigationEnd` events is ineffective because each event instance is unique. Prefer mapping to the URL (e.g., `event.urlAfterRedirects`) and using `takeUntilDestroyed()` / `DestroyRef` (or implement `OnDestroy`) to avoid leaks and make the distinct check meaningful."
}
{
  "path": "public/src/app/applications/projects.component.ts",
  "line": 135,
  "body": "`handleFragment` schedules a `setTimeout` that calls `cd.detectChanges()`, but the timer isn’t tracked/cancelled. If the component is destroyed before the callback runs, this can trigger \"ViewDestroyedError\". Consider avoiding `setTimeout` (use a microtask/zone stabilization) or store/clear the timer in `ngOnDestroy`, and guard change detection after teardown."
}
{
  "path": "public/src/app/applications/projects.component.ts",
  "line": 210,
  "body": "`fetchFOMs()` manually calls `cd.detectChanges()` inside the HTTP subscription, but the request isn’t cancelled on destroy. Navigating away with an in-flight request can result in `detectChanges()` being invoked after the view is destroyed. Pipe the HTTP observable through `takeUntil(this.ngUnsubscribe)` (or use `takeUntilDestroyed`) and/or avoid explicit `detectChanges()` by using `markForCheck()` with OnPush."
}
{
  "path": "public/src/test-setup.ts",
  "line": 5,
  "body": "Importing `@jest/environment` in test setup doesn’t configure Jest to use JSDOM and can fail depending on Jest’s internal package layout. Prefer setting `testEnvironment: 'jsdom'` (or relying on `jest-preset-angular` defaults) and remove this import unless it’s required for a specific side effect."
}
