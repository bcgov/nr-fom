import { SpatialFeatureModule } from './spatial-feature.module';

describe('SpatialFeatureModule', () => {
  it('should be defined', () => {
    const module = new SpatialFeatureModule();
    expect(module).toBeDefined();
  });
});
